
import type { Config } from "tailwindcss";

export default {
	darkMode: ["class"],
	content: [
		"./pages/**/*.{ts,tsx}",
		"./components/**/*.{ts,tsx}",
		"./app/**/*.{ts,tsx}",
		"./src/**/*.{ts,tsx}",
	],
	prefix: "",
	theme: {
		container: {
			center: true,
			padding: '2rem',
			screens: {
				'2xl': '1400px'
			}
		},
		extend: {
			colors: {
				border: 'hsl(var(--border))',
				input: 'hsl(var(--input))',
				ring: 'hsl(var(--ring))',
				background: 'hsl(var(--background))',
				foreground: 'hsl(var(--foreground))',
				primary: {
					DEFAULT: '#789D3C', // Sage Green
					foreground: '#FFFFFF', // White text on primary
					hover: '#6A8D30', // Slightly darker for hover states
				},
				secondary: {
					DEFAULT: '#4A6B2A', // Dark Moss
					foreground: '#FFFFFF',
					hover: '#3A5B1A',
				},
				destructive: {
					DEFAULT: '#C44D58', // Coral Red for alerts
					foreground: '#FFFFFF',
				},
				muted: {
					DEFAULT: '#F9F9F9', // Very light gray
					foreground: '#666666', // Medium gray text
				},
				accent: {
					DEFAULT: '#E8F0DE', // Light sage for accents
					foreground: '#4A6B2A',
				},
				popover: {
					DEFAULT: 'hsl(var(--popover))',
					foreground: 'hsl(var(--popover-foreground))'
				},
				card: {
					DEFAULT: 'hsl(var(--card))',
					foreground: 'hsl(var(--card-foreground))'
				},
				text: '#333333', // Dark gray for text
				alert: '#C44D58', // Coral Red for alert states
			},
			borderRadius: {
				lg: 'var(--radius)',
				md: 'calc(var(--radius) - 2px)',
				sm: 'calc(var(--radius) - 4px)'
			},
			keyframes: {
				'accordion-down': {
					from: {
						height: '0'
					},
					to: {
						height: 'var(--radix-accordion-content-height)'
					}
				},
				'accordion-up': {
					from: {
						height: 'var(--radix-accordion-content-height)'
					},
					to: {
						height: '0'
					}
				}
			},
			animation: {
				'accordion-down': 'accordion-down 0.2s ease-out',
				'accordion-up': 'accordion-up 0.2s ease-out'
			}
		}
	},
	plugins: [require("tailwindcss-animate")],
} satisfies Config;
